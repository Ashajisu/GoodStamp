Figma 파일 GoodStamp.fig를 기준으로 실제 동작하는 모바일 웹서비스를 구현.
서비스명은 "GoodStamp"를 사용한다.

목적:
개인이 iPhone 15에서 홈 화면에 추가하여 PWA 형태로 사용하는 습관 관리 및 보상 서비스다.
빠른 개인 배포가 목적이므로 백엔드 서버와 DB는 사용하지 않는다.
모든 사용자 데이터는 localStorage 또는 IndexedDB에 저장한다.

기술:
- Vue 3
- Vite
- JavaScript
- CSS
- PWA
- GitHub Pages 배포 가능하도록 workflows 구성
- 외부 UI 프레임워크는 사용하지 말고 Figma 디자인을 CSS로 직접 구현

stitch & Figma:
stitch의 AI를 통해 디자인 시안을 구성한다.
Figma 로 export 한다.
첨부한 GoodStamp.fig의 디자인을 최대한 그대로 구현한다.
색상, 폰트 크기, 간격, 카드 모서리, 버튼, 아이콘, 하단 네비게이션 등 시각적 디자인을 임의로 변경하지 않는다.
단, 실제 모바일 사용성을 위해 필요한 경우 터치 영역과 반응형 처리는 개선한다.

화면:
1. 로그인
- 아이디 입력
- 생년월일 입력
- 개인용 서비스이므로 실제 서버 인증은 하지 않는다.
- 최초 로그인 정보를 localStorage에 저장한다.
- 이후에는 자동 로그인한다.

2. 오늘의 할일
- 오늘 날짜의 할일 목록 표시
- 할일 완료/미완료 상태 변경
- 하단 버튼으로 할일 추가
- 오늘의 할일 / 반복 할일 타입 지원
- 시간 알림 최대 3개 설정
- 진행률 체크목록 선택 가능
- 최소 완료 진행률 설정
- 스페셜 할일 지원
- 스페셜 할일 완료 시 "참 잘했어요!" 스탬프 1개 지급
- 스탬프 지급 순간 도장이 찍히는 애니메이션 표시
- 새로고침해도 상태가 유지되어야 한다.

3. 주간 리스트
- 월~일을 표시
- 각 날짜별 완료한 할일 수 / 전체 할일 수 표시
- 해당 날짜에 스탬프가 있는지 표시
- 날짜를 선택하면 해당 날짜의 할일을 확인할 수 있도록 한다.

4. 보상 리스트
- 현재 보유 스탬프 수 표시
- 각 보상에 필요한 스탬프 수 표시
- 보상 목록 추가/수정/삭제
- 교환 버튼 제공
- 교환 시 생년월일 확인
- 생년월일이 일치하면 필요한 수량만큼 스탬프 차감
- 보유 스탬프보다 필요한 수량이 많으면 교환 불가
- 교환 내역도 localStorage에 저장한다.

공통:
- iPhone 15 세로 화면을 최우선으로 디자인한다.
- 모바일 터치에 최적화한다.
- 하단 네비게이션을 사용한다.
- 오늘 / 주간 / 보상 화면 간 이동이 가능해야 한다.
- 스크롤 영역과 고정 영역을 명확하게 구분한다.
- 실제 버튼을 클릭하면 화면 상태가 변경되어야 한다.
- mock 화면이 아니라 실제 동작하는 MVP를 만든다.
- 새로고침 후에도 모든 데이터가 유지되어야 한다.
- 날짜 기준은 사용자의 로컬 날짜를 사용한다.
- 코드에 불필요한 복잡한 추상화를 넣지 않는다.
- 컴포넌트는 재사용 가능한 단위로 적절히 분리한다.

PWA:
- manifest와 service worker를 구성한다.
- iPhone 홈 화면에 추가할 수 있도록 한다.
- standalone 모드로 실행한다.
- 모바일 viewport를 올바르게 설정한다.
- safe-area-inset을 고려한다.

배포:
- GitHub Pages에서 바로 배포할 수 있도록 설정한다.
- Vite의 base 경로를 GitHub Pages 환경에 맞게 설정한다.
- 별도의 서버 없이 동작해야 한다.

구현 순서:
1. Stitch & Figma 화면 디자인
2. 공통 디자인 변수 및 컴포넌트 생성
3. 로그인
4. 오늘의 할일
5. 주간 리스트
6. 보상 리스트
7. localStorage 데이터 관리
8. 스탬프 애니메이션
9. PWA 설정
10. GitHub Pages 배포 설정

각 단계가 끝날 때마다 실제 동작 가능한 상태를 유지한다.